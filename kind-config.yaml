kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane          # Nodo Master che gestisce il cluster
  - role: worker                 # Nodo Worker che esegue i Pod delle applicazioni